[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "ranked-enumeration"
version = "0.1.0"
description = "Ranked enumeration for acyclic conjunctive queries"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
